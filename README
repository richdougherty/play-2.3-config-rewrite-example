A demonstration of how to override configuration values in a Play 2.3 Java application.

This demo app implements WebSphere's XOR "encryption" algorithm, where a string like "{xor}LDo8LTor" will be decrypted to "secret". See [http://strelitzia.net/wasXORdecoder/wasXORdecoder.html].